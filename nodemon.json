{
  "exec": "npm run build && cross-env ENV_SOURCE=file MODE=public NODE_ENV=development USE_HTTPS=true node ./build/index.js",
  "ext": "ts",
  "ignore": [
    ".git",
    "build",
    "node_modules"
  ],
  "watch": [
    "./package.json",
    "./src"
  ]
}
